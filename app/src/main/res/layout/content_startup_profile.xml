<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.androidapp.startlancer.ui.startup.navigation.StartupProfileActivity"
    tools:showIn="@layout/activity_startup_profile">

    <include
        layout="@layout/nav_header"
        android:id="@+id/startup_header" />

    <TextView
        android:id="@+id/startup_openings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Openings"
        android:gravity="center"
        android:layout_below="@+id/startup_header"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textSize="20dp"
        android:textAllCaps="true" />

    <LinearLayout
        android:layout_below="@+id/startup_openings"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ListView
            android:id="@+id/openings_list"
            android:layout_width="fill_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:scrollbars="none"
            android:dividerHeight="1sp"
            android:divider="@color/colorAccent" />

    </LinearLayout>

</RelativeLayout>
